# Copie este arquivo para ".env" (opcional) e preencha.
# Base URL do backend (Java). Exemplo:
# VITE_API_URL=http://localhost:8080
# Ambiente da aplicação (dev | staging | prod)
# Em produção, use VITE_APP_ENV=prod
VITE_APP_ENV=dev
VITE_API_URL=

